[
  {
    "id": "commit_001",
    "title": "GLFW port uses nudgeTransitions",
    "before": "glfwWaitEvents();\nconst bool dirty = !clean.test_and_set();\nif(dirty) {\n    map->renderSync();\n}",
    "after": "glfwWaitEvents();\nconst bool dirty = !clean.test_and_set();\nif(dirty) {\n    const bool needsRerender = map->renderSync();\n    map->nudgeTransitions(needsRerender);\n}",
    "added": "const bool needsRerender = map->renderSync();\nmap->nudgeTransitions(needsRerender);",
    "deleted": "map->renderSync();",
    "is_buggy": true
  }
]